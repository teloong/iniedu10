---
import Layout from '../layouts/Layout.astro';
title: "Tentang IniEdu"
---

<Layout title="Tentang IniEdu">
  <!-- Hero Section -->
  <section class="relative min-h-[60vh] flex flex-col md:flex-row items-center justify-center py-16 px-4 bg-gradient-to-br from-blue-50 via-white to-blue-100 overflow-hidden" data-aos="fade-up">
    <div class="flex-1 z-10 flex flex-col items-start justify-center max-w-xl pl-52">
      <h1 class="text-4xl md:text-5xl font-extrabold mb-4 text-[#232b4d] drop-shadow-lg" data-aos="fade-right">Tentang <span class="bg-gradient-to-r from-blue-500 to-cyan-400 bg-clip-text text-transparent">IniEdu</span></h1>
      <p class="mb-4 text-lg md:text-xl text-gray-700" data-aos="fade-right" data-aos-delay="100">Platform pendidikan masa depan yang memadukan teknologi, kreativitas, dan nilai kemanusiaan untuk generasi Indonesia yang unggul.</p>
      <div class="flex flex-wrap gap-4 mt-4">
        <span class="px-4 py-2 rounded-full bg-gradient-to-r from-blue-400 to-cyan-400 text-white font-semibold shadow-lg group transition-all duration-300 cursor-pointer" data-aos="zoom-in" data-aos-delay="200">#Futuristik</span>
        <span class="px-4 py-2 rounded-full bg-gradient-to-r from-yellow-400 to-orange-400 text-white font-semibold shadow-lg group transition-all duration-300 cursor-pointer" data-aos="zoom-in" data-aos-delay="300">#Inovatif</span>
        <span class="px-4 py-2 rounded-full bg-gradient-to-r from-pink-400 to-purple-400 text-white font-semibold shadow-lg group transition-all duration-300 cursor-pointer" data-aos="zoom-in" data-aos-delay="400">#AksesPendidikan</span>
      </div>
    </div>
    <div class="flex-1 flex justify-center z-10 ml-64" data-aos="zoom-in">
      <img src="https://img.freepik.com/premium-photo/e-learning-class-mobile-phone_38825-113.jpg?semt=ais_hybrid&w=740" alt="Ilustrasi 3D Kelas Online" class="max-w-xs md:max-w-md rounded-2xl shadow-2xl border-8 border-blue-400 transition-all duration-300 hover:shadow-[0_8px_40px_rgba(0,89,255,0.25)] hover:border-blue-500 hover:scale-105 cursor-pointer" />
    </div>
    <div class="absolute top-0 left-0 w-full h-full pointer-events-none select-none opacity-40" style="background: radial-gradient(ellipse at 80% 20%, #7f5cff33 0%, transparent 70%), radial-gradient(ellipse at 20% 80%, #00ffe722 0%, transparent 70%);"></div>
  </section>

  <!-- Visi Misi Card Section -->
  <section class="max-w-4xl mx-auto mt-12 grid md:grid-cols-2 gap-8 px-4" data-aos="fade-up">
    <div class="bg-white rounded-2xl shadow-xl p-8 border-l-4 border-blue-400 flex flex-col items-start group hover:shadow-[0_32px_96px_rgba(0,89,255,0.40)] hover:scale-110 hover:-translate-y-4 hover:ring-8 hover:ring-blue-400 hover:ring-offset-2 hover:border-blue-600 border border-transparent hover:animate-glow transition-all duration-500" data-aos="fade-right">
      <h2 class="text-2xl font-bold mb-2 text-blue-600">Visi</h2>
      <p class="text-gray-700">Menjadi pelopor pendidikan digital yang inklusif, adaptif, dan berdaya saing global untuk seluruh masyarakat Indonesia.</p>
    </div>
    <div class="bg-white rounded-2xl shadow-xl p-8 border-l-4 border-cyan-400 flex flex-col items-start group hover:shadow-[0_32px_96px_rgba(0,89,255,0.40)] hover:scale-110 hover:-translate-y-4 hover:ring-8 hover:ring-blue-400 hover:ring-offset-2 hover:border-blue-600 border border-transparent hover:animate-glow transition-all duration-500" data-aos="fade-left">
      <h2 class="text-2xl font-bold mb-2 text-cyan-600">Misi</h2>
      <ul class="list-disc ml-6 text-gray-700 space-y-2">
        <li>Menyediakan materi dan pembelajaran berkualitas tinggi.</li>
        <li>Mengembangkan fitur interaktif dan inovatif berbasis teknologi.</li>
        <li>Mendorong pemerataan akses pendidikan di seluruh Indonesia.</li>
        <li>Memberikan sertifikasi resmi yang diakui industri.</li>
      </ul>
    </div>
  </section>

  <!-- Value/Highlight Section -->
  <section class="max-w-5xl mx-auto mt-16 px-4 mb-12" data-aos="fade-up">
    <div class="grid md:grid-cols-3 gap-8">
      <div class="bg-gradient-to-br from-blue-100 to-blue-50 rounded-xl p-6 flex flex-col items-center shadow group hover:shadow-[0_32px_96px_rgba(0,89,255,0.40)] hover:scale-110 hover:-translate-y-4 hover:ring-8 hover:ring-blue-400 hover:ring-offset-2 hover:border-blue-600 border border-transparent hover:animate-glow transition-all duration-500" data-aos="zoom-in" data-aos-delay="100">
        <svg width="38" height="38" fill="none" viewBox="0 0 38 38" class="group-hover:animate-pulse drop-shadow-[0_0_8px_rgba(0,89,255,0.7)] transition-all duration-500"><circle cx="19" cy="19" r="19" fill="#009fff" opacity=".12"/><path d="M13 26V14.5a6 6 0 1 1 12 0V26" stroke="#009fff" stroke-width="2" stroke-linecap="round"/><rect x="11" y="26" width="16" height="4" rx="2" fill="#009fff"/></svg>
        <div class="font-bold text-lg mt-3 mb-1 text-blue-700">Materi Berkualitas</div>
        <div class="text-gray-600 text-center">Konten terkurasi oleh pengajar profesional dan praktisi industri.</div>
      </div>
      <div class="bg-gradient-to-br from-yellow-100 to-yellow-50 rounded-xl p-6 flex flex-col items-center shadow group hover:shadow-[0_32px_96px_rgba(0,89,255,0.40)] hover:scale-110 hover:-translate-y-4 hover:ring-8 hover:ring-blue-400 hover:ring-offset-2 hover:border-blue-600 border border-transparent hover:animate-glow transition-all duration-500" data-aos="zoom-in" data-aos-delay="200">
        <svg width="38" height="38" fill="none" viewBox="0 0 38 38" class="group-hover:animate-pulse drop-shadow-[0_0_8px_rgba(0,89,255,0.7)] transition-all duration-500"><circle cx="19" cy="19" r="19" fill="#ffb300" opacity=".12"/><path d="M19 13v8" stroke="#ffb300" stroke-width="2" stroke-linecap="round"/><circle cx="19" cy="25" r="1.5" fill="#ffb300"/></svg>
        <div class="font-bold text-lg mt-3 mb-1 text-yellow-700">Sertifikat Resmi</div>
        <div class="text-gray-600 text-center">Setiap kursus dilengkapi sertifikat digital yang diakui industri.</div>
      </div>
      <div class="bg-gradient-to-br from-pink-100 to-purple-50 rounded-xl p-6 flex flex-col items-center shadow group hover:shadow-[0_32px_96px_rgba(0,89,255,0.40)] hover:scale-110 hover:-translate-y-4 hover:ring-8 hover:ring-blue-400 hover:ring-offset-2 hover:border-blue-600 border border-transparent hover:animate-glow transition-all duration-500" data-aos="zoom-in" data-aos-delay="300">
        <svg width="38" height="38" fill="none" viewBox="0 0 38 38" class="group-hover:animate-pulse drop-shadow-[0_0_8px_rgba(0,89,255,0.7)] transition-all duration-500"><circle cx="19" cy="19" r="19" fill="#e040fb" opacity=".12"/><path d="M12 22l7-7 7 7" stroke="#e040fb" stroke-width="2" stroke-linecap="round"/></svg>
        <div class="font-bold text-lg mt-3 mb-1 text-pink-700">Interaktif & Kolaboratif</div>
        <div class="text-gray-600 text-center">Belajar lebih seru dengan fitur diskusi, tugas, dan komunitas.</div>
      </div>
    </div>
  </section>
<script>
// Efek 3D tilt interaktif pada semua card dengan class group (fix TypeScript lint)
const cards = document.querySelectorAll('.group');
cards.forEach((cardEl) => {
  const card = cardEl as HTMLElement;
  card.style.perspective = '800px';
  card.addEventListener('mousemove', (e) => {
    const event = e as MouseEvent;
    const rect = card.getBoundingClientRect();
    const x = event.clientX - rect.left;
    const y = event.clientY - rect.top;
    const centerX = rect.width / 2;
    const centerY = rect.height / 2;
    const rotateX = ((y - centerY) / centerY) * 10;
    const rotateY = ((x - centerX) / centerX) * 10;
    card.style.transform = `scale(1.08) translateY(-12px) rotateX(${-rotateX}deg) rotateY(${rotateY}deg)`;
    card.style.transition = 'transform 0.1s';
    card.style.boxShadow = '0 0 48px 8px #38bdf8, 0 0 24px 4px #6366f1';
  });
  card.addEventListener('mouseleave', (e) => {
    const event = e as MouseEvent;
    card.style.transform = '';
    card.style.transition = 'transform 0.6s';
    card.style.boxShadow = '';
  });
});
// Efek 3D tilt interaktif pada badge hashtag
const badges = document.querySelectorAll('.flex.flex-wrap.gap-4 span.group');
badges.forEach((badgeEl) => {
  const badge = badgeEl as HTMLElement;
  badge.style.perspective = '600px';
  badge.addEventListener('mousemove', (e) => {
    const event = e as MouseEvent;
    const rect = badge.getBoundingClientRect();
    const x = event.clientX - rect.left;
    const y = event.clientY - rect.top;
    const centerX = rect.width / 2;
    const centerY = rect.height / 2;
    const rotateX = ((y - centerY) / centerY) * 10;
    const rotateY = ((x - centerX) / centerX) * 10;
    badge.style.transform = `scale(1.08) translateY(-4px) rotateX(${-rotateX}deg) rotateY(${rotateY}deg)`;
    badge.style.transition = 'transform 0.1s';
    badge.style.boxShadow = '0 0 24px 4px #38bdf8, 0 0 12px 2px #f472b6';
  });
  badge.addEventListener('mouseleave', () => {
    badge.style.transform = '';
    badge.style.transition = 'transform 0.6s';
    badge.style.boxShadow = '';
  });
});
</script>
</Layout>
